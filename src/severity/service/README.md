# Prediction API

The FastAPI entry point is `severity.service.app:app`. It provides `/health`, `/ready`, and `/v1/predict`. Run commands from the repository root; see the main README for setup and example requests.
